import { createContext } from 'react';

// Notice the use of Pascal convention for const name
// because this will be like a class and not an object
const StoreContext = createContext();

export default StoreContext;
